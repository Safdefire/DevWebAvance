package fr.imt.lgi2p.pcmd;

import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.GenerationType;
import javax.persistence.Id;

@Entity
public class Candidature {

	@Id
	@GeneratedValue(strategy = GenerationType.IDENTITY)
	public String etudiantLogin;
	public String choixMobilite;

	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + ((choixMobilite == null) ? 0 : choixMobilite.hashCode());
		result = prime * result + ((etudiantLogin == null) ? 0 : etudiantLogin.hashCode());
		return result;
	}

	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (obj == null)
			return false;
		if (getClass() != obj.getClass())
			return false;
		Candidature other = (Candidature) obj;
		if (choixMobilite == null) {
			if (other.choixMobilite != null)
				return false;
		} else if (!choixMobilite.equals(other.choixMobilite))
			return false;
		if (etudiantLogin == null) {
			if (other.etudiantLogin != null)
				return false;
		} else if (!etudiantLogin.equals(other.etudiantLogin))
			return false;
		return true;
	}

	public String getEtudiantLogin() {
		return etudiantLogin;
	}

	public void setEtudiantLogin(String etudiantLogin) {
		this.etudiantLogin = etudiantLogin;
	}

	public String getChoixMobilite() {
		return choixMobilite;
	}

	public void setChoixMobilite(String choixMobilite) {
		this.choixMobilite = choixMobilite;
	}

	public Candidature() {
		super();
	}

	public Candidature(Mobilite choixMobilite, Etudiant etudiant) {
		super();
		this.choixMobilite = choixMobilite.getUniversite();
		this.etudiantLogin = etudiant.getLogin();
	}

}
